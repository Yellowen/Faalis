- form_target = Rails.application.routes.url_helpers.send("#{Faalis::Engine.dashboard_namespace}_auth_groups_path".to_sym)
= render partial: 'form', locals: { form_target: form_target, method: :post }
