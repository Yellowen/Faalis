- form_target = Rails.application.routes.url_helpers.send("#{Faalis::Engine.dashboard_namespace}_auth_users_update_path".to_sym, @user.id)
- index = Rails.application.routes.url_helpers.send("#{Faalis::Engine.dashboard_namespace}_auth_users_path".to_sym)
= render partial: 'form', locals: { method: :put, form_target: form_target, index: index }
