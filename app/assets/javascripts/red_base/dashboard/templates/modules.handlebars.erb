<ul>
  <li>{{# link-to 'index' }}<i class="icon-user"></i> <%= _("Dashboard") %>{{/link-to}}</li>
  {{#each}}
  <li>{{# link-to url }} <i {{bind-attr class=icon }}></i> {{ title }}{{/link-to}}</li>
  {{/each}}
</ul>

<div class="brand">
  <h1><%= RedBase::Engine.site_title %></h1>
  <span><%= _("Some slug") %></span>
</div>
