@import "red_base/variables";
@import "red_base/dashboard/ltr/foundation_and_overrides";

@font-face {
  font-family: 'Open Sans';
  src: url(<%= asset_path("OpenSans-Regular.ttf") %>) format('truetype'); /* Saf3â€”5, Chrome4+, FF3.5, Opera 10+ */
}

* {
    font-family: 'Open Sans', serif;
}

input[type="integer"] {
  @include form-element;
}
select {
  @include form-element;
}
select[multiple] {
  min-height: 10em;
}

textarea {
  height: 10em !important;
}
